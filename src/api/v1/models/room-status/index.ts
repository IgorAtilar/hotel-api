export type RoomStatus = {
    id: string;
    name: string;
};

export type CreateRoomStatusInput = {
    name: string;
};

export type UpdateRoomStatusInput = {
    id: string;
    name: string;
};
